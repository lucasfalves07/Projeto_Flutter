name: poliedro_flutter
description: "Projeto Flutter migrado do front em React"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # -------------------------
  # 🔥 Firebase (backend na nuvem)
  # -------------------------
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.3
  firebase_storage: ^13.0.0       # atualizado p/ compatibilidade com core 4.x
  firebase_messaging: ^16.0.0     # atualizado p/ compatibilidade com core 4.x
  file_picker: ^10.3.3
  url_launcher: ^6.3.0

  # -------------------------
  # 💾 Banco local (cache/offline)
  # -------------------------
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3

  # -------------------------
  # 🎨 UI, tema e utilitários
  # -------------------------
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  go_router: ^16.3.0
  shared_preferences: ^2.3.3
  intl: ^0.20.2
  fl_chart: ^1.1.1
  another_flushbar: ^1.12.30
  animations: ^2.0.11
  google_fonts: ^6.2.1
  dynamic_color: ^1.6.6

  # -------------------------
  # 📊 Exportação e relatórios
  # -------------------------
  excel: ^4.0.6
  pdf: ^3.11.1
  printing: ^5.14.0
  share_plus: ^12.0.1

# -------------------------
# 🧪 Testes (TDD/BDD)
# -------------------------
dev_dependencies:
  flutter_test:
    sdk: flutter

  # Mock de autenticação compatível com Firebase 4.x
  firebase_auth_mocks: ^0.15.1

  flutter_gherkin: ^2.0.0
  glob: ^2.1.2

flutter:
  uses-material-design: true

  assets:
    - assets/poliedro-logo.png
